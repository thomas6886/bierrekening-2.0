extends layout
block content
    <script src="http://code.jquery.com/jquery-1.11.3.min.js"></script>
    div.center.totalview
        div.header
           p.large 30 seconds game
        div.info
            p.large Score: #{score}
            p The playing team number: #{team_playing}
            p Team name: #{team_names}

        form(method='post' action='/30seconds/NextRound')
            div.form_box
                each val in words
                    div
                    p.button_word.inner-div1.noselect  #{val}
                    input.center_content.inner-div2(type="checkbox" name= val value="button")
                br
                br
                //This input field is hidden and the gameroomid can be send to get the appropiate data from the server
                input(type="hidden" name="GameRoomId" value = GameRoomId)
                input(type="hidden" name="Round" value =Round)
                input(type="submit" value="NextRound")
        <progress value="0" max="10" id="progressBar"></progress>
        p#time_bar De tijd loopt

        script(src='/javascripts/test_30seconds.js')



    <div class="form-check">
    <input class="form-check-input" type="checkbox" value="" id="defaultCheck1">
    <label class="form-check-label noselect" for="defaultCheck1">
    p checkbox hier asdfjklasdjf fjaklj a
    </label>
    </div>
